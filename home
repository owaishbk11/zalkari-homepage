<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Swarovski Style Animated Header</title>
    <style>
        /* ===== BASE STYLES ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background: #fff;
            color: #000;
            min-height: 200vh; /* For scrolling demo */
        }
        
        /* ===== SWAROVSKI ANIMATED HEADER ===== */
        .swarovski-header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 0, 0, 0.08);
        }
        
        /* Header States */
        .swarovski-header.scrolled {
            transform: translateY(-40px);
            opacity: 0.95;
        }
        
        .swarovski-header.scrolled .main-nav {
            transform: translateY(-100%);
            opacity: 0;
            pointer-events: none;
        }
        
        .swarovski-header.scrolled .logo-container,
        .swarovski-header.scrolled .header-actions {
            transform: translateY(40px);
        }
        
        /* ===== TOP PROMO BAR ===== */
        .promo-bar {
            background: #000;
            color: #fff;
            text-align: center;
            padding: 10px 0;
            font-size: 0.85rem;
            letter-spacing: 1px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        
        .promo-text {
            animation: fadeInUp 0.6s ease;
        }
        
        .promo-bar strong {
            font-weight: 600;
            margin: 0 5px;
        }
        
        /* ===== MAIN HEADER CONTENT ===== */
        .header-main {
            padding: 15px 0;
            transition: padding 0.3s ease;
        }
        
        .header-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 40px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        
        .swarovski-header.scrolled .header-main {
            padding: 8px 0;
        }
        
        /* ===== LOGO ===== */
        .logo-container {
            transition: transform 0.4s ease;
        }
        
        .logo {
            font-size: 1.8rem;
            letter-spacing: 6px;
            font-weight: 300;
            text-transform: uppercase;
            color: #000;
            text-decoration: none;
            display: block;
            position: relative;
        }
        
        .logo:after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 1px;
            background: #000;
            transition: width 0.3s ease;
        }
        
        .logo:hover:after {
            width: 100%;
        }
        
        /* ===== MAIN NAVIGATION ===== */
        .main-nav {
            transition: all 0.4s ease;
            position: relative;
        }
        
        .nav-list {
            display: flex;
            list-style: none;
            gap: 35px;
        }
        
        .nav-item {
            position: relative;
        }
        
        .nav-link {
            color: #000;
            text-decoration: none;
            font-size: 0.9rem;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            font-weight: 300;
            padding: 8px 0;
            position: relative;
            transition: color 0.3s ease;
            display: block;
        }
        
        /* Animated underline */
        .nav-link:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 1px;
            background: #000;
            transition: width 0.3s ease;
        }
        
        .nav-link:hover:after {
            width: 100%;
        }
        
        /* SALE badge */
        .sale-badge {
            background: #000;
            color: #fff;
            font-size: 0.7rem;
            padding: 2px 8px;
            border-radius: 2px;
            margin-left: 5px;
            animation: pulse 2s infinite;
            display: inline-block;
        }
        
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.8; }
            100% { opacity: 1; }
        }
        
        /* ===== DROPDOWN MENUS ===== */
        .dropdown {
            position: absolute;
            top: 100%;
            left: -20px;
            background: #fff;
            min-width: 200px;
            opacity: 0;
            visibility: hidden;
            transform: translateY(10px);
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(0, 0, 0, 0.08);
            z-index: 100;
            padding: 20px 0;
        }
        
        .nav-item:hover .dropdown {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }
        
        .dropdown-item {
            display: block;
            padding: 10px 25px;
            color: #000;
            text-decoration: none;
            font-size: 0.85rem;
            letter-spacing: 1px;
            transition: all 0.2s ease;
            position: relative;
        }
        
        .dropdown-item:before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 0;
            height: 1px;
            background: #000;
            transition: width 0.3s ease;
        }
        
        .dropdown-item:hover {
            padding-left: 35px;
        }
        
        .dropdown-item:hover:before {
            width: 8px;
        }
        
        /* ===== HEADER ACTIONS ===== */
        .header-actions {
            display: flex;
            align-items: center;
            gap: 25px;
            transition: transform 0.4s ease;
        }
        
        .action-link {
            color: #000;
            text-decoration: none;
            font-size: 1.1rem;
            position: relative;
            transition: transform 0.3s ease;
            display: flex;
            align-items: center;
        }
        
        .action-link:hover {
            transform: translateY(-2px);
        }
        
        .action-link.search-trigger {
            cursor: pointer;
        }
        
        /* ===== SEARCH OVERLAY ===== */
        .search-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.98);
            z-index: 2000;
            display: none;
            opacity: 0;
            transition: opacity 0.3s ease;
        }
        
        .search-overlay.active {
            display: block;
            animation: fadeIn 0.3s forwards;
        }
        
        @keyframes fadeIn {
            to { opacity: 1; }
        }
        
        .search-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 90%;
            max-width: 600px;
        }
        
        .search-input {
            width: 100%;
            padding: 20px 0;
            font-size: 2.5rem;
            border: none;
            border-bottom: 2px solid #000;
            background: transparent;
            color: #000;
            outline: none;
            font-weight: 300;
            letter-spacing: 2px;
            transition: border-color 0.3s ease;
        }
        
        .search-input:focus {
            border-bottom-color: #666;
        }
        
        .search-close {
            position: absolute;
            top: 40px;
            right: 40px;
            font-size: 1.5rem;
            cursor: pointer;
            color: #000;
            transition: transform 0.3s ease;
        }
        
        .search-close:hover {
            transform: rotate(90deg);
        }
        
        /* ===== MOBILE MENU BUTTON ===== */
        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            cursor: pointer;
            width: 30px;
            height: 30px;
            position: relative;
            z-index: 1001;
        }
        
        .menu-icon {
            display: block;
            width: 100%;
            height: 2px;
            background: #000;
            position: relative;
            transition: all 0.3s ease;
        }
        
        .menu-icon:before,
        .menu-icon:after {
            content: '';
            position: absolute;
            width: 100%;
            height: 2px;
            background: #000;
            left: 0;
            transition: all 0.3s ease;
        }
        
        .menu-icon:before {
            top: -8px;
        }
        
        .menu-icon:after {
            bottom: -8px;
        }
        
        .mobile-menu-btn.active .menu-icon {
            background: transparent;
        }
        
        .mobile-menu-btn.active .menu-icon:before {
            top: 0;
            transform: rotate(45deg);
        }
        
        .mobile-menu-btn.active .menu-icon:after {
            bottom: 0;
            transform: rotate(-45deg);
        }
        
        /* ===== MOBILE NAVIGATION ===== */
        .mobile-nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #fff;
            z-index: 999;
            transform: translateX(-100%);
            transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            padding: 100px 40px 40px;
            overflow-y: auto;
        }
        
        .mobile-nav.active {
            transform: translateX(0);
        }
        
        .mobile-nav-list {
            list-style: none;
        }
        
        .mobile-nav-item {
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }
        
        .mobile-nav-link {
            display: block;
            padding: 20px 0;
            color: #000;
            text-decoration: none;
            font-size: 1.2rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            font-weight: 300;
            position: relative;
            transition: color 0.3s ease;
        }
        
        .mobile-nav-link:after {
            content: '›';
            position: absolute;
            right: 0;
            top: 50%;
            transform: translateY(-50%);
            font-size: 1.5rem;
            opacity: 0.3;
            transition: transform 0.3s ease;
        }
        
        .mobile-nav-link:hover {
            color: #666;
        }
        
        .mobile-nav-link:hover:after {
            transform: translateY(-50%) translateX(5px);
        }
        
        /* ===== ANIMATIONS ===== */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        /* ===== RESPONSIVE DESIGN ===== */
        @media (max-width: 1024px) {
            .header-container {
                padding: 0 20px;
            }
            
            .nav-list {
                gap: 20px;
            }
        }
        
        @media (max-width: 768px) {
            .mobile-menu-btn {
                display: block;
            }
            
            .main-nav {
                display: none;
            }
            
            .header-actions {
                gap: 20px;
            }
            
            .logo {
                font-size: 1.5rem;
                letter-spacing: 4px;
            }
            
            .promo-bar {
                font-size: 0.75rem;
                height: 35px;
            }
        }
        
        @media (max-width: 480px) {
            .header-container {
                padding: 0 15px;
            }
            
            .logo {
                font-size: 1.3rem;
            }
            
            .header-actions {
                gap: 15px;
            }
            
            .action-link {
                font-size: 1rem;
            }
        }
        
        /* ===== DEMO CONTENT ===== */
        .hero-section {
            height: 100vh;
            background: linear-gradient(135deg, #f5f5f5 0%, #e0e0e0 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 120px;
            text-align: center;
            padding: 0 20px;
        }
        
        .hero-title {
            font-size: 3.5rem;
            letter-spacing: 8px;
            font-weight: 300;
            margin-bottom: 20px;
            text-transform: uppercase;
            animation: fadeInUp 1s ease;
        }
        
        .hero-subtitle {
            font-size: 1.2rem;
            letter-spacing: 3px;
            color: #666;
            animation: fadeInUp 1s ease 0.2s both;
        }
        
        .content-section {
            max-width: 1200px;
            margin: 100px auto;
            padding: 0 20px;
        }
        
        .content-section h2 {
            font-size: 2rem;
            letter-spacing: 4px;
            margin-bottom: 40px;
            font-weight: 300;
            text-transform: uppercase;
        }
        
        .content-section p {
            line-height: 1.8;
            color: #333;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <!-- Swarovski Style Animated Header -->
    <header class="swarovski-header" id="mainHeader">
        <!-- Top Promo Bar -->
        <div class="promo-bar">
            <div class="promo-text">
                <strong>10% OFF</strong> YOUR NEXT PURCHASE • SIGN UP TO OUR NEWSLETTER
            </div>
        </div>
        
        <!-- Main Header -->
        <div class="header-main">
            <div class="header-container">
                <!-- Mobile Menu Button -->
                <button class="mobile-menu-btn" id="mobileMenuBtn">
                    <span class="menu-icon"></span>
                </button>
                
                <!-- Logo -->
                <div class="logo-container">
                    <a href="#" class="logo">SWAROVSKI</a>
                </div>
                
                <!-- Desktop Navigation -->
                <nav class="main-nav">
                    <ul class="nav-list">
                        <li class="nav-item">
                            <a href="#" class="nav-link">Sale <span class="sale-badge">SALE</span></a>
                            <div class="dropdown">
                                <a href="#" class="dropdown-item">All Sale Items</a>
                                <a href="#" class="dropdown-item">Jewelry Sale</a>
                                <a href="#" class="dropdown-item">Watches Sale</a>
                                <a href="#" class="dropdown-item">Accessories Sale</a>
                            </div>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">New In</a>
                            <div class="dropdown">
                                <a href="#" class="dropdown-item">Latest Collections</a>
                                <a href="#" class="dropdown-item">Seasonal Picks</a>
                                <a href="#" class="dropdown-item">Editor's Choice</a>
                            </div>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">Jewelry</a>
                            <div class="dropdown">
                                <a href="#" class="dropdown-item">Necklaces</a>
                                <a href="#" class="dropdown-item">Earrings</a>
                                <a href="#" class="dropdown-item">Rings</a>
                                <a href="#" class="dropdown-item">Bracelets</a>
                            </div>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">Watches</a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">Accessories</a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">Decorations</a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">Gifts</a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">Created Diamonds</a>
                        </li>
                        <li class="nav-item">
                            <a href="#" class="nav-link">World of Swarovski</a>
                        </li>
                    </ul>
                </nav>
                
                <!-- Header Actions -->
                <div class="header-actions">
                    <a href="#" class="action-link search-trigger" id="searchTrigger">
                        <i class="fas fa-search"></i>
                    </a>
                    <a href="#" class="action-link">
                        <i class="fas fa-user"></i>
                    </a>
                    <a href="#" class="action-link">
                        <i class="fas fa-shopping-bag"></i>
                    </a>
                </div>
            </div>
        </div>
    </header>
    
    <!-- Search Overlay -->
    <div class="search-overlay" id="searchOverlay">
        <div class="search-container">
            <input type="text" class="search-input" placeholder="SEARCH SWAROVSKI...">
            <div class="search-close" id="searchClose">✕</div>
        </div>
    </div>
    
    <!-- Mobile Navigation -->
    <nav class="mobile-nav" id="mobileNav">
        <ul class="mobile-nav-list">
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">Sale <span class="sale-badge">SALE</span></a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">New In</a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">Jewelry</a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">Watches</a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">Accessories</a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">Decorations</a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">Gifts</a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">Created Diamonds</a>
            </li>
            <li class="mobile-nav-item">
                <a href="#" class="mobile-nav-link">World of Swarovski</a>
            </li>
        </ul>
    </nav>
    
    <!-- Demo Content -->
    <main>
        <section class="hero-section">
            <div>
                <h1 class="hero-title">Zalkari</h1>
                <p class="hero-subtitle">Premium Silver Jewelry</p>
                <p style="margin-top: 30px; color: #999; letter-spacing: 2px;">Scroll down to see header animation</p>
            </div>
        </section>
        
        <section class="content-section">
            <h2>Experience Swarovski Animation</h2>
            <p>This header replicates the exact animation behavior of Swarovski.com. When you scroll down:</p>
            <ul style="list-style: none; margin: 20px 0;">
                <li style="padding: 10px 0; border-bottom: 1px solid #eee;">✓ Promo bar slides up and hides</li>
                <li style="padding: 10px 0; border-bottom: 1px solid #eee;">✓ Navigation menu elegantly disappears</li>
                <li style="padding: 10px 0; border-bottom: 1px solid #eee;">✓ Logo and actions shift upward smoothly</li>
                <li style="padding: 10px 0; border-bottom: 1px solid #eee;">✓ Header becomes more compact (reduces height)</li>
                <li style="padding: 10px 0;">✓ Scroll back up to see the reverse animation</li>
            </ul>
            <p>Additional features include:</p>
            <ul style="list-style: none; margin: 20px 0;">
                <li style="padding: 8px 0;">• Animated dropdown menus on hover</li>
                <li style="padding: 8px 0;">• Smooth search overlay with fade-in animation</li>
                <li style="padding: 8px 0;">• Mobile-responsive hamburger menu with transform animation</li>
                <li style="padding: 8px 0;">• SALE badge with pulsing animation</li>
                <li style="padding: 8px 0;">• Hover effects with animated underlines</li>
            </ul>
        </section>
        
        <div style="height: 800px; background: #f9f9f9; display: flex; align-items: center; justify-content: center; color: #999; letter-spacing: 2px;">
            Keep scrolling to see the header transform
        </div>
    </main>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <script>
        // Header Scroll Animation
        const header = document.getElementById('mainHeader');
        let lastScroll = 0;
        
        window.addEventListener('scroll', () => {
            const currentScroll = window.pageYOffset;
            
            if (currentScroll > 100) {
                header.classList.add('scrolled');
                if (currentScroll > lastScroll) {
                    // Scrolling down
                    header.style.transform = 'translateY(-100%)';
                } else {
                    // Scrolling up
                    header.style.transform = 'translateY(0)';
                }
            } else {
                header.classList.remove('scrolled');
                header.style.transform = 'translateY(0)';
            }
            
            lastScroll = currentScroll;
        });
        
        // Search Functionality
        const searchTrigger = document.getElementById('searchTrigger');
        const searchOverlay = document.getElementById('searchOverlay');
        const searchClose = document.getElementById('searchClose');
        const searchInput = document.querySelector('.search-input');
        
        searchTrigger.addEventListener('click', (e) => {
            e.preventDefault();
            searchOverlay.classList.add('active');
            searchInput.focus();
        });
        
        searchClose.addEventListener('click', () => {
            searchOverlay.classList.remove('active');
        });
        
        searchOverlay.addEventListener('click', (e) => {
            if (e.target === searchOverlay) {
                searchOverlay.classList.remove('active');
            }
        });
        
        // Mobile Menu
        const mobileMenuBtn = document.getElementById('mobileMenuBtn');
        const mobileNav = document.getElementById('mobileNav');
        
        mobileMenuBtn.addEventListener('click', () => {
            mobileMenuBtn.classList.toggle('active');
            mobileNav.classList.toggle('active');
            document.body.style.overflow = mobileNav.classList.contains('active') ? 'hidden' : '';
        });
        
        // Close mobile menu when clicking links
        document.querySelectorAll('.mobile-nav-link').forEach(link => {
            link.addEventListener('click', () => {
                mobileMenuBtn.classList.remove('active');
                mobileNav.classList.remove('active');
                document.body.style.overflow = '';
            });
        });
        
        // Dropdown hover delay
        const navItems = document.querySelectorAll('.nav-item');
        navItems.forEach(item => {
            let timeout;
            
            item.addEventListener('mouseenter', () => {
                clearTimeout(timeout);
                const dropdown = item.querySelector('.dropdown');
                if (dropdown) {
                    dropdown.style.transition = 'all 0.3s ease';
                }
            });
            
            item.addEventListener('mouseleave', () => {
                const dropdown = item.querySelector('.dropdown');
                if (dropdown) {
                    timeout = setTimeout(() => {
                        dropdown.style.transition = 'all 0.2s ease';
                    }, 300);
                }
            });
        });
    </script>
</body>
</html>
