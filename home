<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zalkari | Premium Silver Jewelry</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* ===== BASE STYLES ===== */
        :root {
            --primary: #930190;
            --text-dark: #1a1a1a;
            --text-light: #666;
            --background: #ffffff;
            --border: #e8e8e8;
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            background: var(--background);
            color: var(--text-dark);
            line-height: 1.6;
            overflow-x: hidden;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        img {
            max-width: 100%;
            height: auto;
            display: block;
        }

        /* ===== TOP BAR ===== */
        .top-bar {
            background: #000;
            color: #fff;
            padding: 12px 0;
            font-size: 0.875rem;
            border-bottom: 1px solid #333;
        }

        .top-bar-content {
            max-width: 1440px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .top-bar-left {
            display: flex;
            gap: 24px;
        }

        .top-bar-link {
            color: #fff;
            text-decoration: none;
            font-size: 0.875rem;
            letter-spacing: 0.5px;
            transition: var(--transition);
        }

        .top-bar-link:hover {
            opacity: 0.8;
        }

        /* ===== MAIN HEADER ===== */
        .main-header {
            background: var(--background);
            padding: 20px 0;
            border-bottom: 1px solid var(--border);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .header-content {
            max-width: 1440px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 28px;
            font-weight: 300;
            letter-spacing: 3px;
            color: var(--text-dark);
        }

        .logo span {
            color: var(--primary);
        }

        .main-nav {
            display: flex;
            gap: 32px;
        }

        .nav-link {
            font-size: 0.9375rem;
            color: var(--text-dark);
            letter-spacing: 1px;
            transition: var(--transition);
            position: relative;
            padding: 8px 0;
        }

        .nav-link:hover {
            color: var(--primary);
        }

        .nav-link.active {
            color: var(--primary);
        }

        .nav-link.active:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 1px;
            background: var(--primary);
        }

        .header-icons {
            display: flex;
            gap: 20px;
            align-items: center;
        }

        .icon-link {
            color: var(--text-dark);
            font-size: 1.125rem;
            transition: var(--transition);
        }

        .icon-link:hover {
            color: var(--primary);
            transform: translateY(-2px);
        }

        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--text-dark);
            cursor: pointer;
        }

        /* ===== HERO SECTION ===== */
        .hero-section {
            position: relative;
            min-height: 90vh;
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, #f8f8f8 0%, #f0f0f0 100%);
            overflow: hidden;
        }

        .hero-content {
            max-width: 1440px;
            margin: 0 auto;
            padding: 0 24px;
            position: relative;
            z-index: 2;
            width: 100%;
        }

        .hero-text {
            max-width: 500px;
        }

        .hero-subtitle {
            font-size: 1rem;
            letter-spacing: 3px;
            color: var(--primary);
            margin-bottom: 16px;
            text-transform: uppercase;
        }

        .hero-title {
            font-size: 3.5rem;
            font-weight: 300;
            line-height: 1.1;
            margin-bottom: 24px;
            letter-spacing: 2px;
        }

        .hero-description {
            font-size: 1.125rem;
            color: var(--text-light);
            margin-bottom: 32px;
            max-width: 400px;
        }

        .hero-buttons {
            display: flex;
            gap: 16px;
        }

        .btn {
            padding: 14px 32px;
            border: none;
            font-size: 0.9375rem;
            font-weight: 500;
            letter-spacing: 1px;
            cursor: pointer;
            transition: var(--transition);
            text-transform: uppercase;
        }

        .btn-primary {
            background: var(--primary);
            color: white;
        }

        .btn-primary:hover {
            background: #7a0178;
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: transparent;
            border: 1px solid var(--text-dark);
            color: var(--text-dark);
        }

        .btn-secondary:hover {
            background: var(--text-dark);
            color: white;
        }

        .hero-image {
            position: absolute;
            right: 24px;
            top: 50%;
            transform: translateY(-50%);
            width: 50%;
            max-width: 700px;
            z-index: 1;
        }

        .hero-image img {
            width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.1);
        }

        /* ===== FEATURED PRODUCTS ===== */
        .section {
            padding: 80px 24px;
            max-width: 1440px;
            margin: 0 auto;
        }

        .section-header {
            text-align: center;
            margin-bottom: 48px;
        }

        .section-title {
            font-size: 2rem;
            font-weight: 300;
            letter-spacing: 2px;
            margin-bottom: 16px;
            position: relative;
            display: inline-block;
        }

        .section-title:after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 2px;
            background: var(--primary);
        }

        .section-subtitle {
            color: var(--text-light);
            font-size: 1rem;
            letter-spacing: 1px;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 32px;
            margin-top: 48px;
        }

        .product-card {
            background: white;
            border: 1px solid var(--border);
            transition: var(--transition);
            overflow: hidden;
        }

        .product-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .product-image {
            height: 320px;
            overflow: hidden;
            background: #f8f8f8;
        }

        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s ease;
        }

        .product-card:hover .product-image img {
            transform: scale(1.05);
        }

        .product-info {
            padding: 24px;
        }

        .product-category {
            font-size: 0.875rem;
            color: var(--text-light);
            letter-spacing: 1px;
            margin-bottom: 8px;
            text-transform: uppercase;
        }

        .product-name {
            font-size: 1.125rem;
            font-weight: 500;
            margin-bottom: 8px;
            letter-spacing: 0.5px;
        }

        .product-price {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 16px;
        }

        .current-price {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--primary);
        }

        .original-price {
            font-size: 1rem;
            color: var(--text-light);
            text-decoration: line-through;
        }

        .product-tags {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .tag {
            padding: 4px 12px;
            background: #f8f8f8;
            color: var(--text-light);
            font-size: 0.75rem;
            border-radius: 4px;
            letter-spacing: 0.5px;
        }

        .tag.sale {
            background: #ffe6e6;
            color: #d32f2f;
        }

        /* ===== CATEGORIES ===== */
        .categories-section {
            background: #f8f8f8;
        }

        .categories-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 24px;
        }

        .category-card {
            position: relative;
            height: 400px;
            overflow: hidden;
            border-radius: 8px;
        }

        .category-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s ease;
        }

        .category-card:hover img {
            transform: scale(1.1);
        }

        .category-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
            padding: 32px;
            color: white;
        }

        .category-title {
            font-size: 1.5rem;
            margin-bottom: 8px;
            font-weight: 300;
            letter-spacing: 2px;
        }

        /* ===== NEWSLETTER ===== */
        .newsletter-section {
            background: var(--text-dark);
            color: white;
            text-align: center;
        }

        .newsletter-content {
            max-width: 600px;
            margin: 0 auto;
        }

        .newsletter-title {
            font-size: 2rem;
            font-weight: 300;
            letter-spacing: 2px;
            margin-bottom: 16px;
        }

        .newsletter-description {
            color: #ccc;
            margin-bottom: 32px;
            font-size: 1.125rem;
        }

        .newsletter-form {
            display: flex;
            max-width: 400px;
            margin: 0 auto;
        }

        .newsletter-input {
            flex: 1;
            padding: 14px 20px;
            border: none;
            font-size: 1rem;
            color: var(--text-dark);
        }

        .newsletter-input:focus {
            outline: none;
        }

        .newsletter-btn {
            padding: 14px 32px;
            background: var(--primary);
            color: white;
            border: none;
            font-size: 0.9375rem;
            font-weight: 500;
            letter-spacing: 1px;
            cursor: pointer;
            transition: var(--transition);
        }

        .newsletter-btn:hover {
            background: #7a0178;
        }

        /* ===== FOOTER ===== */
        .main-footer {
            background: #1a1a1a;
            color: #ccc;
            padding: 60px 24px 30px;
        }

        .footer-content {
            max-width: 1440px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-column h4 {
            color: white;
            font-size: 1.125rem;
            font-weight: 500;
            margin-bottom: 24px;
            letter-spacing: 1px;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: #999;
            font-size: 0.9375rem;
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: white;
            padding-left: 8px;
        }

        .social-links {
            display: flex;
            gap: 16px;
            margin-top: 24px;
        }

        .social-link {
            width: 36px;
            height: 36px;
            background: #333;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition);
        }

        .social-link:hover {
            background: var(--primary);
            transform: translateY(-2px);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid #333;
            color: #666;
            font-size: 0.875rem;
        }

        /* ===== RESPONSIVE DESIGN ===== */
        @media (max-width: 1024px) {
            .hero-title {
                font-size: 2.5rem;
            }
            
            .hero-image {
                width: 45%;
            }
            
            .main-nav {
                gap: 24px;
            }
        }

        @media (max-width: 768px) {
            .mobile-menu-btn {
                display: block;
            }
            
            .main-nav {
                position: fixed;
                top: 0;
                left: -100%;
                width: 80%;
                height: 100vh;
                background: white;
                flex-direction: column;
                padding: 100px 40px 40px;
                transition: var(--transition);
                z-index: 1000;
            }
            
            .main-nav.active {
                left: 0;
            }
            
            .hero-section {
                min-height: 70vh;
            }
            
            .hero-content {
                text-align: center;
            }
            
            .hero-text {
                max-width: 100%;
            }
            
            .hero-image {
                position: relative;
                width: 100%;
                max-width: 400px;
                margin: 40px auto 0;
                transform: none;
                top: 0;
            }
            
            .hero-buttons {
                justify-content: center;
                flex-wrap: wrap;
            }
            
            .products-grid {
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            }
        }

        @media (max-width: 480px) {
            .hero-title {
                font-size: 2rem;
            }
            
            .section {
                padding: 60px 20px;
            }
            
            .newsletter-form {
                flex-direction: column;
            }
            
            .newsletter-input,
            .newsletter-btn {
                width: 100%;
            }
            
            .footer-content {
                grid-template-columns: 1fr;
                text-align: center;
            }
            
            .social-links {
                justify-content: center;
            }
        }

        /* ===== ANIMATIONS ===== */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in {
            animation: fadeInUp 0.6s ease forwards;
        }
    </style>
</head>
<body>
    <!-- Top Bar -->
    <div class="top-bar">
        <div class="top-bar-content">
            <div class="top-bar-left">
                <a href="#" class="top-bar-link">Free Shipping Worldwide</a>
                <a href="#" class="top-bar-link">Easy Returns</a>
                <a href="#" class="top-bar-link">Expert Support</a>
            </div>
            <div class="top-bar-right">
                <a href="#" class="top-bar-link">Get 10% Off First Order</a>
            </div>
        </div>
    </div>

    <!-- Main Header -->
    <header class="main-header">
        <div class="header-content">
            <button class="mobile-menu-btn">
                <i class="fas fa-bars"></i>
            </button>
            
            <a href="#" class="logo">ZALK<span>ARI</span></a>
            
            <nav class="main-nav">
                <a href="#" class="nav-link active">Home</a>
                <a href="#" class="nav-link">Shop</a>
                <a href="#" class="nav-link">Collections</a>
                <a href="#" class="nav-link">Best Sellers</a>
                <a href="#" class="nav-link">New Arrivals</a>
                <a href="#" class="nav-link">Journal</a>
                <a href="#" class="nav-link">Contact</a>
            </nav>
            
            <div class="header-icons">
                <a href="#" class="icon-link">
                    <i class="fas fa-search"></i>
                </a>
                <a href="#" class="icon-link">
                    <i class="fas fa-user"></i>
                </a>
                <a href="#" class="icon-link">
                    <i class="fas fa-shopping-bag"></i>
                </a>
            </div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-content">
            <div class="hero-text fade-in">
                <div class="hero-subtitle">Premium Silver Jewelry</div>
                <h1 class="hero-title">Elegance Meets Craftsmanship</h1>
                <p class="hero-description">
                    Discover handcrafted silver jewelry that blends traditional artistry with contemporary design. Each piece tells a unique story.
                </p>
                <div class="hero-buttons">
                    <button class="btn btn-primary">Shop Collection</button>
                    <button class="btn btn-secondary">View Lookbook</button>
                </div>
            </div>
            <div class="hero-image fade-in">
                <img src="https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" 
                     alt="Premium Silver Jewelry">
            </div>
        </div>
    </section>

    <!-- Featured Products -->
    <section class="section">
        <div class="section-header">
            <h2 class="section-title">Featured Collections</h2>
            <p class="section-subtitle">Handpicked selections of our finest jewelry</p>
        </div>
        
        <div class="products-grid">
            <!-- Product 1 -->
            <div class="product-card fade-in">
                <div class="product-image">
                    <img src="https://images.unsplash.com/photo-1599643478518-a784e5dc4c8f?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" 
                         alt="Geometric Necklace">
                </div>
                <div class="product-info">
                    <div class="product-category">Necklaces</div>
                    <h3 class="product-name">Geometric Silver Pendant</h3>
                    <div class="product-price">
                        <span class="current-price">₹2,375</span>
                        <span class="original-price">₹4,751</span>
                    </div>
                    <div class="product-tags">
                        <span class="tag sale">50% Off</span>
                        <span class="tag">Best Seller</span>
                    </div>
                </div>
            </div>
            
            <!-- Product 2 -->
            <div class="product-card fade-in">
                <div class="product-image">
                    <img src="https://images.unsplash.com/photo-1605100804763-247f67b3557e?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" 
                         alt="Minimalist Ring">
                </div>
                <div class="product-info">
                    <div class="product-category">Rings</div>
                    <h3 class="product-name">Minimalist Silver Band</h3>
                    <div class="product-price">
                        <span class="current-price">₹1,979</span>
                        <span class="original-price">₹3,959</span>
                    </div>
                    <div class="product-tags">
                        <span class="tag sale">50% Off</span>
                        <span class="tag">New Arrival</span>
                    </div>
                </div>
            </div>
            
            <!-- Product 3 -->
            <div class="product-card fade-in">
                <div class="product-image">
                    <img src="https://images.unsplash.com/photo-1515562141207-7a88fb7ce338?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" 
                         alt="Statement Earrings">
                </div>
                <div class="product-info">
                    <div class="product-category">Earrings</div>
                    <h3 class="product-name">Diamond Drop Earrings</h3>
                    <div class="product-price">
                        <span class="current-price">₹4,091</span>
                        <span class="original-price">₹8,183</span>
                    </div>
                    <div class="product-tags">
                        <span class="tag sale">50% Off</span>
                        <span class="tag">Limited Edition</span>
                    </div>
                </div>
            </div>
            
            <!-- Product 4 -->
            <div class="product-card fade-in">
                <div class="product-image">
                    <img src="https://images.unsplash.com/photo-1594575111051-4e4d4c5c9b48?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" 
                         alt="Modern Bracelet">
                </div>
                <div class="product-info">
                    <div class="product-category">Bracelets</div>
                    <h3 class="product-name">Twisted Silver Bracelet</h3>
                    <div class="product-price">
                        <span class="current-price">₹3,101</span>
                        <span class="original-price">₹6,203</span>
                    </div>
                    <div class="product-tags">
                        <span class="tag sale">50% Off</span>
                        <span class="tag">Editor's Pick</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Categories Section -->
    <section class="section categories-section">
        <div class="section-header">
            <h2 class="section-title">Shop By Category</h2>
            <p class="section-subtitle">Find your perfect piece</p>
        </div>
        
        <div class="categories-grid">
            <!-- Category 1 -->
            <div class="category-card fade-in">
                <img src="https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" 
                     alt="Necklaces">
                <div class="category-overlay">
                    <h3 class="category-title">Necklaces</h3>
                    <p>Elegant pendants and chains</p>
                </div>
            </div>
            
            <!-- Category 2 -->
            <div class="category-card fade-in">
                <img src="https://images.unsplash.com/photo-1605100804763-247f67b3557e?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" 
                     alt="Rings">
                <div class="category-overlay">
                    <h3 class="category-title">Rings</h3>
                    <p>From minimal to statement</p>
                </div>
            </div>
            
            <!-- Category 3 -->
            <div class="category-card fade-in">
                <img src="https://images.unsplash.com/photo-1594575111051-4e4d4c5c9b48?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" 
                     alt="Earrings">
                <div class="category-overlay">
                    <h3 class="category-title">Earrings</h3>
                    <p>Studs, drops, and hoops</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Newsletter -->
    <section class="section newsletter-section">
        <div class="newsletter-content">
            <h2 class="newsletter-title">Stay Updated</h2>
            <p class="newsletter-description">
                Subscribe to receive updates on new collections, exclusive offers, and style inspiration.
            </p>
            <form class="newsletter-form">
                <input type="email" placeholder="Enter your email address" class="newsletter-input" required>
                <button type="submit" class="newsletter-btn">Subscribe</button>
            </form>
        </div>
    </section>

    <!-- Footer -->
    <footer class="main-footer">
        <div class="footer-content">
            <div class="footer-column">
                <h4>Shop</h4>
                <ul class="footer-links">
                    <li><a href="#">All Jewelry</a></li>
                    <li><a href="#">New Arrivals</a></li>
                    <li><a href="#">Best Sellers</a></li>
                    <li><a href="#">Sale</a></li>
                    <li><a href="#">Gift Cards</a></li>
                </ul>
            </div>
            
            <div class="footer-column">
                <h4>Information</h4>
                <ul class="footer-links">
                    <li><a href="#">About Us</a></li>
                    <li><a href="#">Contact</a></li>
                    <li><a href="#">Shipping Info</a></li>
                    <li><a href="#">Returns & Exchanges</a></li>
                    <li><a href="#">Privacy Policy</a></li>
                </ul>
            </div>
            
            <div class="footer-column">
                <h4>Customer Service</h4>
                <ul class="footer-links">
                    <li><a href="#">FAQ</a></li>
                    <li><a href="#">Size Guide</a></li>
                    <li><a href="#">Jewelry Care</a></li>
                    <li><a href="#">Order Tracking</a></li>
                    <li><a href="#">Contact Support</a></li>
                </ul>
            </div>
            
            <div class="footer-column">
                <h4>Connect With Us</h4>
                <p style="color: #999; margin-bottom: 20px;">Follow us for style inspiration and updates</p>
                <div class="social-links">
                    <a href="#" class="social-link">
                        <i class="fab fa-instagram"></i>
                    </a>
                    <a href="#" class="social-link">
                        <i class="fab fa-facebook-f"></i>
                    </a>
                    <a href="#" class="social-link">
                        <i class="fab fa-pinterest"></i>
                    </a>
                    <a href="#" class="social-link">
                        <i class="fab fa-twitter"></i>
                    </a>
                </div>
            </div>
        </div>
        
        <div class="footer-bottom">
            <p>&copy; 2023 Zalkari. All rights reserved. | Premium Silver Jewelry Store</p>
        </div>
    </footer>

    <script>
        // Mobile Menu Toggle
        const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
        const mainNav = document.querySelector('.main-nav');
        
        mobileMenuBtn.addEventListener('click', () => {
            mainNav.classList.toggle('active');
            mobileMenuBtn.innerHTML = mainNav.classList.contains('active') 
                ? '<i class="fas fa-times"></i>' 
                : '<i class="fas fa-bars"></i>';
        });
        
        // Close mobile menu when clicking outside
        document.addEventListener('click', (e) => {
            if (!mainNav.contains(e.target) && !mobileMenuBtn.contains(e.target)) {
                mainNav.classList.remove('active');
                mobileMenuBtn.innerHTML = '<i class="fas fa-bars"></i>';
            }
        });
        
        // Scroll animations
        const fadeElements = document.querySelectorAll('.fade-in');
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.animationDelay = `${entry.target.dataset.delay || 0}s`;
                    entry.target.classList.add('fade-in');
                }
            });
        }, {
            threshold: 0.1
        });
        
        fadeElements.forEach((el, index) => {
            el.dataset.delay = index * 0.1;
            observer.observe(el);
        });
        
        // Newsletter form
        const newsletterForm = document.querySelector('.newsletter-form');
        newsletterForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const email = newsletterForm.querySelector('input[type="email"]').value;
            alert(`Thank you for subscribing with ${email}! You'll receive 10% off your first order.`);
            newsletterForm.reset();
        });
        
        // Add to Cart buttons
        document.querySelectorAll('.btn-primary').forEach(btn => {
            if (btn.textContent.includes('Shop') || btn.textContent.includes('View')) return;
            
            btn.addEventListener('click', function() {
                const productName = this.closest('.product-card').querySelector('.product-name').textContent;
                const originalText = this.textContent;
                
                this.textContent = 'Added to Cart!';
                this.style.background = '#4CAF50';
                
                setTimeout(() => {
                    this.textContent = originalText;
                    this.style.background = '';
                }, 2000);
                
                // In Shopify, you would add actual cart logic here
            });
        });
    </script>
</body>
</html>
